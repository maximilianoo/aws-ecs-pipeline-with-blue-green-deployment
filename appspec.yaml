version: 0.0
Resources:
 - TargetService:
     Type: AWS::ECS::Service
     Properties:
       TaskDefinition: prod-tf
       LoadBalancerInfo:
         ContainerName: prod-container
         ContainerPort: 8080